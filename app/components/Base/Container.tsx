import { styled } from "~/styles/stitches.config";

export const Container = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  overflowX: 'hidden',
  alignItems: 'center',
  // maxWidth: '100vw',
  // padding: '0 20px',
})

  // display: 'grid',
  // gridColumn: '2 / auto',